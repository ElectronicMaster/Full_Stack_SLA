// trim - will take of space infront string
var str = "       apple, mango, grapes"
str = str.trim()
console.log(str);

// toUpperCase
console.log(str.toUpperCase());

// toLowerCase
console.log(str.toLowerCase());

// slice - str.slice(startIndex,endIndex);
console.log(str.slice(0,8));